# Minimal, clean requirements for your notebook imports
# Note: PyTorch may be better installed via conda (see instructions below)

pandas>=2.0
numpy>=1.24
emoji>=2.0
matplotlib>=3.5
seaborn>=0.12
scikit-learn>=1.2
google_play_scraper>=1.0.0

# Hugging Face / Transformers stack
transformers>=4.40.0
huggingface-hub>=0.18.0
tokenizers>=0.12.1
accelerate>=0.18.0
peft>=0.10.0
safetensors>=0.2.0

# PyTorch: keep a loose constraint; prefer installing PyTorch using conda/pytorch channel
torch>=2.0.0

# Optional (useful utilities)
# datasets>=2.0.0
 
# TensorFlow (CPU) and ONNX for inference
tensorflow-cpu>=2.19.0
onnx>=1.14.0
onnxruntime>=1.15.0

# Web frameworks for serving models locally
flask>=2.3
fastapi>=0.95
uvicorn[standard]>=0.22.0
